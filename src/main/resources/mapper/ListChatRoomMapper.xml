<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.norinori777.repository.ListChatRoomMapper">
	<select id="selectChatRooms" resultType="jp.norinori777.domain.model.Room.Room">
		select 
			roomId,
			roomName, 
			roomDescription,
            roomOwnerId,
            create_at, 
			update_at
		from m_room
		left innter join t_room_user on m_room.roomId = t_room_user.roomId
		where t_room_user.userId = #{userId} OR room_owner_id = #{userId}
	</select>
</mapper>