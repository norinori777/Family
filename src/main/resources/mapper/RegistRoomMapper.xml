<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.norinori777.infrastructure.datasource.RegistRoomMapper">
	<insert id="insertChatRoom" useGeneratedKeys="true" keyProperty="roomId" keyColumn="room_id">
		insert into m_room(
			room_name,
			room_description,
			room_owner_id,
			state,
			create_at,
			update_at
		) values (
			#{roomName},
			#{roomDescription},
			#{roomOwnerId},
			0,
			now(),
			now()
		)
	</insert>

	<insert id="insertChatRoomMember" parameterType="jp.norinori777.domain.model.Room.ChatRoomMemberUser">
		insert into t_room_member(
			room_id,
			user_id,
			state,
			create_at,
			update_at
		) values (
			#{roomId},
			#{userId},
			0,
			now(),
			now()
		)
	</insert>
</mapper>